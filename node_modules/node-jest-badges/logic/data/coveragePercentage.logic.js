"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPercentage = void 0;
var summaryKeys_constant_1 = require("../../constants/summaryKeys.constant");
var getTotalPercentage = function (summary) {
    var result = summaryKeys_constant_1.summaryKeys
        .map(function (k) { return summary.total[k].pct || 0; })
        .reduce(function (a, b) { return a + b; }, 0) / summaryKeys_constant_1.summaryKeys.length;
    return Math.round((result + Number.EPSILON) * 100) / 100;
};
var getPercentage = function (summary, key) {
    if (key === 'jest coverage') {
        return getTotalPercentage(summary);
    }
    var value = summary.total[key].pct;
    if (value === undefined) {
        console.info("No value for key '".concat(key, "' found in coverage report"));
    }
    return value;
};
exports.getPercentage = getPercentage;
